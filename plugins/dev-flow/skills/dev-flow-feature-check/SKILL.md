---
name: dev-flow-feature-check
description: Run deterministic Dev Flow completeness checks.
---

Use only Dev Flow MCP. Call `dev_flow_next`; only when it returns `feature-check` call `dev_flow_feature_check`. Do not synthesize missing evidence.
