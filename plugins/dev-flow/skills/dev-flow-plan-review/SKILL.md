---
name: dev-flow-plan-review
description: Perform the distinct pre-implementation plan review.
---

Use only Dev Flow MCP. Call `dev_flow_next`; only when it returns `plan_review` record evidence with `reviewType: "plan"`. It cannot complete code-review.
