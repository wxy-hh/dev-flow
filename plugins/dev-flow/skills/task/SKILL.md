---
name: task
description: 开任务并分级 start。触发：开任务、开始功能、task、df-task、dev-flow-task。
---

仅使用 Dev Flow MCP。先分类并 `dev_flow_start`，再调用 `dev_flow_next`，只执行它返回的唯一动作。v1 does not integrate OpenSpec；已有规格仅作需求输入。
