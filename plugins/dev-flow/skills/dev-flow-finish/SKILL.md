---
name: dev-flow-finish
description: Finalize a complete Dev Flow feature before any Git write.
---

Use only Dev Flow MCP. Call `dev_flow_next`; only when it returns `finalize` use `dev_flow_finalize`. Only after logic-complete may Git write operations be considered.
