---
name: dev-flow-code-review
description: Perform post-implementation diff review through Dev Flow MCP.
---

Use only Dev Flow MCP. Call `dev_flow_next` first and act only when it returns `code_review`; record code evidence with `reviewType: "code"`. Code-review is distinct from plan-review and cannot complete it.
