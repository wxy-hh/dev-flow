---
name: df-plan
description: 按当前 Dev Flow 路线产出实现计划。用户说写计划、实现计划、writing-plans、df-plan、dev-flow-plan 时使用。当 dev_flow_next 要求 plan 相关步骤时使用。
---

仅使用 Dev Flow MCP 与其资产。调用 `dev_flow_next`，只执行它返回的唯一动作；在记录对应步骤前先登记已更新的 artifact。
