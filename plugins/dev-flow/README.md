# Dev Flow plugin

This directory is the self-contained Dev Flow plugin package distributed by the
Claude Code and Codex marketplaces in this repository.

It includes prebuilt `dist/` entry points. Consumers do not run `npm install`.
