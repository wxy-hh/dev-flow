# 交接：下一代 dev-flow 重构进行中（2026-08-20）

> 2026-08-20 验收后更新：T0~T9 全部完成并验收闭环，唯一剩余 T10 第一批总验收。本文只记"当前状态与下一步"，设计细节读锚点文档，别在本文找规格。

## 一句话现状

下一代工作流（Claude Code 插件）T0~T9 全部完成并验收闭环：单测 218/218 全绿，`npm run verify` + verify-t3~t8 全量串行回归全绿（含 t6 场景⑤ 超时挂死确认）；T7/T8 已接线进 mcp-server.ts（工具面 2，server 名 `df`）；P95=17.9ms ≤30ms；坑清单 95+5 条全显式结局，仅 T-17 待 T10 人审。**唯一剩余任务 T10 第一批总验收**，判据见 `docs/2026-08-19-MVP-实施计划.md` §7。分支 `next`，**所有代码未提交**（用户手动提交制，当前回归全绿是好的提交点）。

## 锚点文档（规格与结论都在这里）

- `docs/2026-08-19-下一代工作流设计方向.md` —— 方向合同（设计宪法/机制定义/砍留）
- `docs/2026-08-19-MVP-实施计划.md` —— 实施计划（§6 任务分解、§7 验收判据、§0 spike 硬约束、附录裁决记录）
- `docs/2026-08-19-spike-结论.md` —— T0 实证（hookSpecificOutput 格式/.cjs/fs.writeSync/P95 基线）
- `docs/2026-08-19-坑清单核对表.md` —— 95+5 条全显式结局（55 覆盖/31 机制已砍失效/8 未吸收），仅 T-17 待 T10 人审

## 进度表（终态）

| 任务 | 状态 |
| --- | --- |
| T0 spike / T9 坑清单 | ✅ 完成并验收 |
| T1 骨架 / T2 状态 / T3 SessionStart / T4 写门禁 / T5 用户通道 | ✅ 完成并逐个验收 |
| T6 done MCP 工具 + PostToolUse 验收记账 | ✅ 完成并验收：verify-t6 五场景全过；场景⑤ 超时挂死实证 exitReason=timeout + backgroundTaskId 落账、done 不误放行 |
| T7 自动 commit | ✅ 完成并接线：`readEvents → planAutoCommit → execAutoCommit` 挂在 mcp-server.ts done 成功路径，skipped/failed 写 audit.warning（verify-t7 6 断言绿） |
| T8 status 查询 | ✅ 完成并注册进 `df` server TOOLS 表（verify-t8 7 断言绿，≤500 字符渲染） |
| 全量回归 | ✅ 单测 218/218；`npm run verify`（T1）+ verify-t3（3 场景）/t4（7 断言）/t5（5 断言）/t6（7 断言）/t7（6 断言）/t8（7 断言）串行全绿 |
| 性能 | ✅ build/bash-fastpath-p95.txt p95=17.9ms ≤30ms |
| T10 第一批总验收 | ⏳ 唯一剩余任务（判据见计划 §7） |

## 下一步：T10 第一批总验收

判据 3（坑清单全绿或显式标记）、判据 4（P95 ≤30ms）已满足；判据 1/2 只能靠真实使用攒数据，判据 5 差一次整环墙钟实测。做法：

1. **真实使用攒 5 个 XS 任务**（判据 1/2）：在真实任务上逐个记交互次数（≤1 次/任务）、零卡点、证据链完整；同时观察 XS 任务零仪式走完、越界（含敏感路径）可见可拦。
2. **补判据 5 计时**：改一行 hook 源码 → build → sandbox 真机验证的整环墙钟实测，要求 <1 分钟。
3. **人审 T-17**：对照计划 §8.7 冷冻点判据（基准全绿 + 5 个真实任务）人审核销，坑清单即全绿。
4. 五条判据全满足后提醒用户提交代码。

## 工作规则（用户定的，接续必须遵守）

- **用户手动提交一切代码**，agent 不做任何 git mutation。提交前若报 lock 错误：删 `.git/index.lock`（0 字节残留仍在）。
- 执行派子代理，主会话只做审查/监督/验收；验收必须真跑测试与场景，不接受"应该可以"。
- 全中文文档与注释；简洁直白、反口号、真实场景导向。
- 新增机制先对照两份设计文档，没有设计依据不加。

## 环境关键事实

- 验证环：`claude -p --plugin-dir plugins/dev-flow` 驱动 sandbox/work（固定验证仓，`scripts/sandbox-reset.sh` 一键重置）；改 hook 代码新会话 0 步生效，无发版无装包。
- 本机 claude 未登录 Anthropic，走 Kimi provider；settings 结构已迁到顶层 env 段 + cc-switch 代理（127.0.0.1:15721）。
- 模型行为方差：Kimi 时而真调工具时而只描述，场景脚本内建重试 ≤3；意图块/门禁行为目前只在 Kimi 上实证过（计划 §8 已知边界）。
- 单测链：esbuild bundle `test/*.test.ts` → `node --test build/test/*.test.cjs`，零新增依赖。

## 遗留与风险

- 模型行为仅 Kimi 实证（计划 §8.8）：意图块"读懂 deny 并改正"只在 Kimi 上验过，Anthropic 未验；T10 有条件时用官方登录环境抽跑。
- 交互模式长期会话 hook 热加载未实证（§8.9）：spike 只实证 `-p` 非交互"新会话 0 步生效"。
- 解释器任意写入（`python -c` 等）是 Bash 启发式的语义盲区（§8.2）：无"不触发事件的写入路径"，残余缺口仅属蓄意绕过类，按合同 10.4 承认防不住。
- 第一批无连败锁定、无 finalize 审计渲染、无评审（§8.3），均第二批；坑清单 8 条"未吸收"对应的覆盖机制属第二批。
- 坑清单 T-17 待 T10 人审（冷冻点判据）。
- 并行跑验证脚本会互相清 sandbox 状态（踩过），所有 verify-*.sh 串行执行。

## Suggested skills

- `code-review` —— 提交前按"双轴"（守仓库规范/忠实实现计划）审查全部未提交 diff。
- `diagnosing-bugs` —— 攒 XS 数据期间遇场景失败时的排查循环。
- `writing-plans` —— 若 T10 验收暴露需要重新拆解任务时。
