# Dev Flow 仪式减法优化路线图（2026-08-15）

- **定位**：过程计划（非合同）。记录从当前架构审查轮收尾到「仪式成本与治理价值再平衡」的完整路径，按依赖关系排序。
- **修订记录**：v2（2026-08-15）——交叉评审外部模型优化建议后合并三项（1.6 全路线枚举测试、2.2 评测基准雏形、5.1 WIP commit 策略档）与两项轻量工程项（1.7 文档单点生成、1.8 只读路径缓存）；评估不采纳五项（见文末「决策状态」）。
- **前置条件**：`docs/2026-08-13-架构审查报告.md` 的 9 个候选全部完成（当前工作区 80 文件、净删约 5250 行，测试全绿但未提交）。
- **三个初衷**（排序与取舍的唯一判据）：
  1. 小任务不过度治理；
  2. 高风险任务不能轻易越级；
  3. 规则长期维护而不持续膨胀。

## 排序原则

1. **测量先行**：所有行为干预之前，先有测量仪（ceremony budget、surface-check、枚举测试），否则干预效果无法归因；
2. **风险分层**：零风险（测量/纪律）→ 低风险（策略调整）→ 中风险（interface 变更）→ 高风险（动核心不变量），逐层推进；
3. **干预独立成版**：每次行为干预单独发版、单独验证，一次只改变一个变量；
4. **数据驱动升级**：只有低风险方案被数据证明不足时，才升级到高风险方案（如 quick lock → provisional）；
5. **外部建议先评审再采纳**：新点子必须过「现有机制能否吸收 / 删什么来换 / 预算是否够」三问，评审结论记入决策状态表。

## 依赖关系总览

```text
阶段 0 收尾发版（基线锁定）
  ├─→ 1.1 ceremony budget 埋点 ───────────┐（测量仪）
  ├─→ 1.2 surface-check 复杂度预算 ───────┤（测量仪）
  ├─→ 1.6 全路线枚举测试 ─────────────────┤（验证仪：合同性质 CI）
  ├─→ 1.7 文档单点生成 ───────────────────┤（消除文档漂移）
  ├─→ 1.3 hook 性能 / 1.4 token 面收敛
  │     / 1.8 只读路径缓存 ───────────────┤（工程债）
  └─→ 1.5 纪律条款（additive-only + 减法协议）──┐
                                              ▼
                 阶段 2 真实项目验证（2.1 三任务 → 2.2 基准雏形）
                                              │
      ┌───────────────────┬───────────────────┴───────────────────┐
      ▼                   ▼                                       ▼
阶段 3.1 boundaryAudit   阶段 3.2 quick lock                   阶段 3.3 scope drift
XS/S 默认处置            （依赖 3.1 的审计形态）                   （行为级越级检测）
      │                   │                                       │
      ▼                   ▼                                       ▼
阶段 4.1 信号-事实绑定    阶段 7.2 provisional                   阶段 4.2 finalize 级别审计
（向下从严）              （仅当 3.2 数据不足才做）                  （非阻塞报告）
                                              │
                                              ▼
                         阶段 5.1 WIP commit 策略档（工程师体验）
                                              │
                                              ▼
                         阶段 6 结构收敛（6.1 交互族 → 6.2 工具面）
                                              │
                                              ▼
                         阶段 7.1 错误码族化（紧跟 6.2，不拖延）
                                              │
                                              ▼
                         阶段 8 复盘与预算刷新（每版本例行）
```

---

## 阶段 0：基线锁定（当前轮收尾）

**0.1 收尾架构审查轮并发版**

- 内容：当前 9 候选全量测试通过后提交（中文 Conventional Commit）→ `version:sync` → `build` → 发版。
- 前置：无。
- 理由：80 文件未提交的基线不能继续漂移；所有后续工作必须站在干净基线上。
- 完成标准：`npm test` 全绿 + 版本号落盘 + dist 同步。

## 阶段 1：测量与纪律（零/低风险，发版后立即）

### 1.1 ceremony budget 埋点（治理成本度量）

- 风险：**零**（纯测量，不改任何行为）。
- 内容：status 投影与最终报告增加「本次任务：工具调用 N 次、用户交互 M 次、证据 K 件」；目标线写入 contract（XS ≤ 2 交互 / ≤ 10 调用；L ≤ 8 交互 / ≤ 40 调用），超线在 finalize 报告显式提示。
- 前置：0.1。
- 为什么最先：它是后续所有干预（3.x/7.2）的**测量仪**；没有它，真实项目验证只有"感觉"没有"数据"。
- 验证：单测覆盖统计字段；真实任务观察输出。

### 1.2 surface-check 复杂度预算（CI 门禁）

- 风险：**零**（纯测量 + 断言）。
- 内容：`scripts/surface-check.mjs` 纳入 `npm run build:check`：公共工具 ≤ 50、错误码 ≤ 35 族、core 模块 ≤ 70、交互类型 ≤ 5 族。先立基线，超限必须显式更新预算并说明删除项。
- 前置：0.1；与 1.1 独立可并行。
- 为什么：这是"规则不膨胀"从愿望变成机器可验证事实的唯一手段。

### 1.3 hook 性能（工程债）

- 风险：低（行为不变）。
- 内容：六事件每次 spawn Node 进程的开销——状态读取按 revision/mtime 缓存、健康信号 15 分钟窗口内内存缓存不重复写盘、事件账本批量写。目标：P95 单事件 < 20ms（可测量后再定）。
- 前置：0.1。
- 理由：这是"理念一流、工程化不足"最直接的体感证据；每次工具调用几十 ms 的 hook 开销是真实摩擦。

### 1.4 token 面收敛

- 风险：低。
- 内容：mutation 响应统一走 compact 投影（`server.ts` 已有 `compactMutationResult`），事实登记等路径不再返回全量 FeatureState；配合 8-13 清单第 6 条的批量登记落地。
- 前置：0.1（8-13 清单第 6 条已在当前轮或随其后完成）。

### 1.5 纪律条款（additive-only schema + 减法变更协议）

- 风险：零（文档纪律）。
- 内容：写入 CLAUDE.md/AGENTS.md——
  - FeatureState 字段只允许新增（带默认值），禁止改名/删字段/改类型，永不做 v6 迁移器；
  - 新增任何控制/工具/错误码/交互前必须回答三问：①现有机制能否吸收？②删除了什么来换？③复杂度预算是否还够。
- 前置：无。
- 理由：任何系统都会在迭代中膨胀，除非"删除"被做成强制流程。

### 1.6 全路线枚举测试（policy 性质测试，新增）

- 风险：**零**（纯测试，不改行为）。
- 内容：对 policy 纯函数层（`route.ts` / `obligations.ts` / `evidence.ts`）做 `level × topology × behaviorChange × changeSurface × 风险标签 × controlEnhancements` 子空间的**全组合性质测试**，断言：
  1. 单调性：增强永不削弱（`applyControlEnhancements` 输出 ≥ 基线）；
  2. 拓扑下限：任意分类满足 `assertTopologyLevel`；
  3. 路线可编译：`compileOrderedRoute` / `routeDefinitionForFeature` 永不产出空路线或不可执行步骤序列；
  4. 义务可执行：每个控制必然派生可执行义务（approval/review/verification/rollback/checkpoint）；
  5. 依赖闭包：planReview / unit-chain / operational-strategy 必然自动补 formal plan + trace。
- 前置：0.1（policy 层当前已是纯函数，随时可加）。
- 为什么：把"机器合同"从工程测试升级为**可证明性质**——合同违反在 CI 被抓住，而不是在真实项目里爆；直接服务初衷三。
- 验证：新增 `tests/unit/policy-properties.test.mjs`，纳入 `npm run test:unit`。

### 1.7 文档与基线单点生成（消除文档漂移，新增）

- 风险：**零**（纯读取，不引入生成链）。
- 内容：文档与基线从**真实代码**派生，而不是手写对照——
  - 工具清单/输入 schema 从 `dispatch.ts` 的 `toolSchemas` 生成（一个脚本的事），替代手写"40 工具"式文档；
  - surface-check 基线（1.2）直接从 `toolSchemas` + 错误码 grep + 模块计数提取；
  - 错误码注册表从源码统一提取。
- 前置：0.1；与 1.2 共享同一提取脚本。
- 明确不做：**不引入治理 DSL / codegen 生成 TS 类型**——DSL 本身成为需要维护的新合同，生成代码要 review，调试穿透两层，与"零运行时依赖、薄工具链"精神冲突（见决策状态）。

### 1.8 只读路径重复读取缓存（新增）

- 风险：低-中（缓存引入新鲜度语义，必须按 revision/mtime/配置 sha256 失效）。
- 内容：先测量 `deriveReviewInput`（review-jobs.ts:195-273）在 status/next 链路上被触发的真实成本（8-13 审查"成本值得量一下"），确认后再做进程内缓存：状态按 revision 缓存、project config 按 sha256 缓存、governed-root 指纹按变更检测缓存；**只缓存只读路径，mutation 永不走缓存**。
- 前置：0.1；测量先行（8-13 审查候选 8 同款纪律：先测频率再决定是否动手）。
- 验证：测量脚本记录 before/after 耗时；缓存失效语义单测。

## 阶段 2：真实项目验证（有界实验，只记录不优化）

**2.1 三任务验证 + 发现三分法**

- 内容：带 1.1 埋点跑 3 个真实任务（2 个 XS/S + 1 个 M），采集调用数/交互数/卡点轮次（翻源码、重复 doctor、重试循环均记录）。
- 前置：1.1（必须有数据采集器）；时间盒一周。
- 发现处置三分法：
  | 类型 | 判定 | 处理 |
  | --- | --- | --- |
  | 当前轮回归 | 之前能走通的行为变了 | **立即修**（bug-fix 批，单独提交），修完重跑确认 |
  | 独立缺陷 | 与当前轮和计划干预都无关 | 小则修，大则登记 `.scratch/` 排期 |
  | 干预目标 | 正是 3.x/4.x/7.2 要消灭的摩擦 | **登记不修**，留给对应干预，避免重复劳动 |
- 完成标准：回归清零、基线数据落盘（作为所有干预的"before"）。

**2.2 评测基准雏形（golden tasks，新增）**

- 风险：零（测试资产）。
- 内容：从 2.1 的真实任务**沉淀**为可复现基准——
  - golden tasks：XS/S/M 各 1-2 个真实任务脚本化（含预期路径）；
  - 越级攻击用例：声明 XS 实际跨组件、伪造 boundaryAudit 条目、模型转述自答、绕过 answer 直调内部 seam；
  - 记录指标：合规率、交互数、工具调用数、卡点轮次、漏检率。
- 前置：2.1（先有真实任务，再沉淀成用例）。
- 为什么：把"领先"从文档自述变成**可证明数据**；同时是 1.1 ceremony budget 的正式化，此后每版本回归跑一遍，用数据判断"变重了还是变轻了"。
- 验证：基准可复现（同一 fixture 两次运行结果一致）。

## 阶段 3：低风险干预批（数据支持的第一个干预，单独成版）

### 3.1 boundaryAudit XS/S 默认处置

- 风险：低（策略调整，纯 policy 函数 + 一个交互）。
- 内容：`assertBoundaryAuditComplete`（route.ts:374）对 XS/S 允许 `scanned: [六类], items: []` + 一次"确认无未处置边界"（绑定 search-absent 事实或并入 route-confirmation）；M/L 保持逐项。
- 前置：2.1（数据确认 XS/S 前置摩擦确实存在）；**先于 3.2**（先定审计形态，再动锁定流程）。
- 验证：单测 + 1 个真实 XS 任务，对比 2.1 的 before 数据。

### 3.2 quick lock（lazy lock 第一步：快速锁定）

- 风险：低-中（保留"lock 前置"不变量，只降输入负担）。
- 内容：XS lock 输入降到最低（3 个 signals + 无风险声明 + 一次确认），**不动** write-gate 写 seam、不引入 provisional 状态机。
- 前置：3.1（建立在新审计形态上）。
- 验证：真实 XS 任务对比 2.1 数据；确认越级防线行为不变（回归测试锁定 `assertTopologyLevel`、`RECLASSIFICATION_DOWNGRADE_FORBIDDEN` 路径）。

### 3.3 scope drift 检测（行为级越级检测）

- 风险：低。
- 内容：implementation 中模型第一次写到 `scope.inScope` 之外的文件时触发一次范围漂移交互：纳入（→ 重新分类，只允许向上）或排除。write-gate 已有路径，加比对即可。
- 前置：2.1；与 3.1/3.2 独立（无依赖），可与批 3 同版或单独。
- 理由：这是唯一基于**实际行为**的防越级防线（XS/S 无 unit-chain 时），比声明检查强一个量级。

## 阶段 4：防越级加固（策略层，低成本）

### 4.1 信号-事实绑定（向下从严、向上宽松）

- 风险：低。
- 内容：声明低于 M 的每个信号（changeSurface/behaviorChange/topology/unitCount）必须绑定至少一条 current 仓库事实引用；声明 M/L 的宽松（升级不需证据，降级才需要）。一行校验逻辑，把谎报成本从 0 提到"必须登记能撑住声明的事实"。
- 前置：3.3（行为检测先于声明检测，可获越级频率数据）；可与 3.x 同批或独立。
- 验证：单测覆盖"XS 声明无事实 → 拒绝/升级"。

### 4.2 finalize 级别审计（非阻塞报告）

- 风险：零（只读报告）。
- 内容：finalize 对比实际变更面（文件数、跨组件、是否动 contract/共享文件）与声明级别，不一致时交付快照给一行显式警告。审计而非门禁——不增加阻塞，让瞒报事后可见。
- 前置：3.3（可选，独立性强）。
- 验证：构造"声明 XS 实际跨组件"的 fixture 断言报告输出。

## 阶段 5：工作流自然化（工程师体验，新增阶段）

### 5.1 WIP commit 策略档（允许 commit、禁止 push）

- 风险：中（git 门禁语义变化，牵动交付快照与 reconcile）。
- 内容：project config 增加严格策略档；**feature branch 内允许 WIP commit**（logic-complete 前），push / merge / publish 仍拦截：
  - 交付快照基于 `baseHead` 的 diff，分支内 commit 不破坏 reverse patch；
  - `git-reconciliation` 配合处理人工 commit（现役机制已覆盖）；
  - 策略档默认关闭（现有行为不变），显式开启才放宽。
- 前置：2.2（基准中有"工程师中途 commit"用例可验证）；与 3.x/4.x 独立。
- 为什么：logic-complete 前拦一切 git 写是当前对真实工程师**最不自然**的一点；这是四个候选体验项（WIP commit / worktree 并行 / CLI / dashboard）中唯一进路线图的（其余见决策状态）。
- 验证：golden task 用例 + git 门禁回归测试；真实任务对比交互数。

## 阶段 6：结构收敛（中成本 interface 变更）

### 6.1 交互族收敛（9 → ≤5）

- 风险：中（interface 变更，技能/文档/测试联动）。
- 内容：decision-ratification 并入 grill；acceptance-confirmation 并入 verification 交互；workspace-ownership 收窄为懒触发（8-13 清单第 1 条已定方向）。目标：grill / approval / ownership / rollback / route-confirmation 五种，其余降为参数。
- 前置：0.1（当前轮候选 1 已把 13 kind 统一到 answer()，本项只是合并 kind，不重做 plumbing）。
- 验证：全量测试 + 真实任务交互次数对比。

### 6.2 工具面收敛（50 → ~30）

- 风险：中（工具名被 8 个 SKILL.md 引用）。
- 内容：review job 一族 6-7 个收敛为 `dev_flow_review_job { action: claim|submit|sample|release }`；rollback 三工具、实现单元三工具同理。每少一个工具 = 少一套 schema/校验/文档/测试/技能说明。
- 前置：6.1（先并 kind 再并工具，避免双重迁移）；当前轮候选 2/3 已满足报告自己定的"候选 2 落地后再评估"条件。
- 验证：8 个 SKILL.md 同步更新 + routes/interop 全绿。

## 阶段 7：高成本收敛与高风险升级

### 7.1 错误码族化（332 → ~35 族 + 参数化细节）

- 风险：高（一次性迁移，现有测试/技能/doctor 引用具体码）。
- 内容：`APPROVAL_EVENT_CONSUMED` / `APPROVAL_REPLY_MISMATCH`… 收敛为 `{ code: "APPROVAL_INVALID", params: { why: "event-consumed" } }`，信息量不减，枚举/测试/文档缩小一个量级。
- 前置：6.2（工具面稳定后**立即**做，不拖延——错误码横跨 core/MCP/hooks/skills/docs 所有层，拖得越久测试与技能引用的具体码越多，迁移成本越高）。
- 验证：全量测试 + 迁移清单核对（grep 旧码零残留）。

### 7.2 provisional lazy lock（真·惰性，唯一的高风险状态机变更）

- 风险：**高**（动"lock 是唯一写 seam"核心不变量：write-gate、route-workflow、schema、task/grillme 技能、status 投影、错误路径）。
- 内容：`dev_flow_start` 后允许 provisional-XS 起步（写文件、跑验证），lock 推迟到第一次 governed write（write-gate 触发轻量分类）或模型主动调用；**provisional 只允许从 XS 起步且只允许向上**。
- 前置：3.2（quick lock）的数据证明"还不够懒"才做；且 2.1 基线数据 + 3.1/3.2 干预后数据齐备。
- 硬约束：先在 policy 层写 `provisionalXSLock` 纯函数 + 单测，验证与 `assertTopologyLevel`、`RECLASSIFICATION_DOWNGRADE_FORBIDDEN` 的交互，再动 write-gate。
- 验证：专项验证"越级防线未开洞"（provisional 期间出现 S+ 信号必须能强制先 lock）。

## 阶段 8：复盘与预算刷新（长期纪律，每版本例行）

- 内容：
  1. 每版本末跑 `surface-check` 并对比上一版基线（工具/错误码/模块数）；
  2. 复盘 ceremony budget 数据：XS/S 实际交互数与目标线的差距；
  3. 跑一遍 2.2 评测基准（合规率/交互数/漏检率趋势）；
  4. 触发条件回顾：若"模型为了走完流程而走流程"（编造 audit 条目、无脑点确认）成为常态，优先做减法而非加校验。
- 前置：1.1、1.2、1.6、2.2（测量仪长期在线）。

---

## 总体节奏表

| 批次 | 内容 | 风险 | 依赖 | 验证方式 |
| --- | --- | --- | --- | --- |
| 批 0 | 当前轮收尾 + 发版 | — | 无 | `npm test` 全绿 |
| 批 1 | 1.1 ceremony budget + 1.2 surface-check + 1.6 枚举测试 + 1.7 文档单点生成 + 1.3 hook 性能 + 1.4 token 收敛 + 1.8 只读路径缓存 + 1.5 纪律条款 | 零/低 | 批 0 | 单测 + 埋点生效 + 性质测试绿 |
| 批 2 | 2.1 真实项目验证（3 任务 + 三分法）+ 2.2 基准雏形 | — | 批 1 | 基线数据落盘 + 基准可复现 |
| 批 3 | 3.1 boundaryAudit 默认处置 + 3.2 quick lock + 3.3 scope drift | 低-中 | 批 2（3.2 依赖 3.1） | 真实任务对比 before/after |
| 批 4 | 4.1 信号-事实绑定 + 4.2 finalize 级别审计 | 低 | 批 3（可选） | 单测 + fixture |
| 批 5 | 5.1 WIP commit 策略档 | 中 | 批 2（2.2 用例） | golden task + 门禁回归 |
| 批 6 | 6.1 交互族收敛 + 6.2 工具面收敛 | 中 | 批 0（候选 1/2/3 已满足前置） | 全量测试 + SKILL 同步 |
| 批 7 | 7.1 错误码族化 + 7.2 provisional（仅数据需要时） | 高 | 批 6；7.2 依赖批 3 数据 | 专项回归 + 越级防线测试 |
| 批 8 | 每版本例行复盘与预算刷新 | — | 1.1、1.2、1.6、2.2 | 版本对比报告 |

## 关键纪律（贯穿全程）

1. **一次只改变一个变量**：每次行为干预独立成版、独立验证，数据才可归因；
2. **干预目标不提前修**：验证中发现的、属于计划干预范围的摩擦，登记 `.scratch/` 不修；
3. **provisional 是最后手段**：只有 quick lock + boundaryAudit 默认处置被数据证明不足时才动核心不变量；
4. **测量仪长期在线**：ceremony budget、surface-check、枚举测试、评测基准永不拆除，它们是"不膨胀"的看门狗；
5. **新增面必须带删除**：任何新工具/新交互/新模块，同一批必须说明删除了什么（1.5 三问的执行化）。

---

## 决策状态（v2 外部建议评审记录）

本表记录对外部模型优化建议（2026-08-15 交叉评审）的采纳结论；正文保留最终决定，不因后续讨论改写。

| 外部建议 | 结果 | 当前依据 |
| --- | --- | --- |
| 全路线枚举测试（policy 性质测试） | **采纳** → 1.6 | 纯函数层可穷举；合同违反在 CI 拦截，直接服务初衷三 |
| golden tasks 评测基准 | **采纳** → 2.2 | ceremony budget 的正式化；"领先"需要可证明数据 |
| WIP commit 策略档（禁 push） | **采纳** → 5.1 | logic-complete 前拦一切 git 写是最大体验摩擦；交付快照基于 baseHead diff 不受影响 |
| 文档漂移 → 从 toolSchemas 生成文档/基线 | **部分采纳** → 1.7 | 问题真实；只做轻量读取器，不引入 DSL/codegen（见下条） |
| 单一事实源 DSL 生成 TS 类型/MCP schema | **不采纳** | codegen 引入新维护面：DSL 本身成新合同、生成代码需 review、调试穿透两层，与"零依赖薄工具链"精神冲突；TS 类型直接消费 contract.json 已是单源 |
| 拆分 god module（state-store/review-jobs/rollback） | **暂缓** | 与 8-13 审查"rollback.ts 拆分属 churn 暂缓"结论一致；state-store 是 57 个测试文件的测试中心，拆它先拆测试面；先做 1.8 缓存，按 deletion test 标准再评估 |
| git worktree 隔离并行 | **原型研究，不进路线图** | 与底线"同一时刻单 active feature"冲突，需先改底线；跨 worktree 合并审查/验证是 6 个月量级大表面 |
| dashboard（终端/网页） | **不采纳** | 营销面非治理面，是新的维护表面；等稳定契约版本后按采用需求再议 |
| 组织级 policy 继承 | **推迟** | 多级策略合并是另一个量级的合同扩展；等稳定契约版本与评测基准落地后 |
| 错误码族化时机 | **修正** → 7.1 | 不放 12 个月末；紧跟工具面收敛（6.2）之后立即做，拖延只会让迁移成本更高 |

---

*本文档为过程计划，完成后可归档至 `docs/plans/` 或删除（参照仓库对 docs/plans/ 的定位：非合同）。*
