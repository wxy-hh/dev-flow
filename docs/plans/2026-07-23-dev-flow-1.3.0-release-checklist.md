# 1.3.0 发布检查清单

## 自动化

- [x] `npm run test:unit`（含 recovery / adapter / status / reclassify）
- [x] `npm run test:e2e`（含 reclassify-standard-to-light）
- [x] `npm test`（version + build + typecheck + unit + e2e + build:check）

## 人工回放（发布阻断；需在真实 Claude / Codex 宿主完成）

使用真实请求：紫微白昼结果页对齐八字磨玻璃 + 截图 + 参考页 + DESIGN.md。

| 检查项 | Claude | Codex | 备注 |
| --- | --- | --- | --- |
| 默认 light-m 或 XS/S，无解释不进 standard-M | | | |
| 强制 standard 时 grill ≤3、无主题重复 | | | |
| 「继续」不改 revision，返回 Q/gate hint | | | |
| 已登记 requirements heredoc 含 apps/ 不误拦 | | | |
| 写 state/control 始终拦截 | | | |
| 「太重了改 light」在安全边界内成功 | | | |

回放通过后才 tag / 发布 marketplace 1.3.0。
