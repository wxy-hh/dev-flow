# Dev Flow 坑清单核对表（初稿）

> - **日期**：2026-08-19（T9 任务，与 T0 spike 并行）
> - **状态**：2026-08-19 已拍板 8 条（N-1~N-5、T-13、GPT-011/T-09、8-12-1，见 §9）；**2026-08-20 按真机验证证据全量核销**——状态列三态闭环（覆盖/机制已砍失效/未吸收），仅剩 T-17 待 T10（冷冻点判据人审）；覆盖机制属第二批的条目显式标为"未吸收（覆盖机制属第二批，第一批未建）"；NEW-1~NEW-3 为新版自踩坑（§10，NEW-4 与 NEW-1 同源不重复建单）
> - **目的**：把旧版（main 分支，已全部删除）踩过的每个坑映射到新版设计，保证"每个旧坑在新版有明确归宿"，T10 第一批验收判据之一 = 本表全绿或全部显式标记（MVP 实施计划 §5、§7.3）

## 0. 读我

### 引用约定

- **合同** = `docs/2026-08-19-下一代工作流设计方向.md`（方向合同，§x.y 指其章节）
- **计划** = `docs/2026-08-19-MVP-实施计划.md`（MVP 实施计划，§x.y 指其章节）
- **出处** = 坑来源（清单编号 / 旧测试路径 `tests/…`）

### 三种结局（每条必须落其一，没有第四种"忘了"）

| 结局 | 含义 | 写法要求 |
| --- | --- | --- |
| **覆盖** | 新版机制兜住该坑 | 引用合同/计划具体小节 |
| **机制已砍失效** | 旧机制在新版被删除，坑随之失效 | 写明砍了什么 |
| **未吸收** | 新版没兜住 | 写明理由与风险，留待人审 |

### 统计（2026-08-20 核销后，正文表格 92 + 新版自踩坑 3 = 95 条；§7 未吸收 5 条另计）

- 覆盖 **55**、机制已砍失效 **31**、未吸收 **8**、待审 **1**（T-17，待 T10 冷冻点判据人审）
- 未吸收 8 条 = C 类（8-12-7/8-12-9/8-12-15/DF-01/GPT-004/T-04/T-05/T-07）：覆盖机制属第二批（评审/finalize 审计），第一批未建，显式标记而非遗漏（2026-08-20 已裁决）
- §7 N-1~N-3 未吸收（2026-08-19 已拍板接受，标 ⚠）；N-4/N-5 已裁决补机制、转入覆盖（正文 8-12-10b、T-11/T-16 相应段落已闭环）
- 置信度标注：**高** = 出处与覆盖机制都能明确指认；**中** = 出处明确但覆盖判断有取舍；**低** = 边缘条目，可直接删

---

## 1. 来自 8-12 优化清单（18 条，其中 §10 拆为 10a/10b 两条）

出处：`docs/8-12-优化清单.md`（对 5.0 动态治理机制评审，含决策状态表）

| # | 坑描述 | 出处 | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 8-12-1 | grill 决策的 A/B/C 选项与推荐理由全由模型生成，机制不校验 framing 质量（模型错框架则机制放大错误）；boundaryAudit 的 evidenceRef 只做形式校验 | 8-12 §1（含决策状态表"分拆采纳"） | **覆盖**：合同 §6.5 提问质量合同——非问不可的问题必须带事实、冲突、选项（A/B/C）与推荐，能用工具查明的绝不问人；§7 grill 保留为"零提问默认 + 一轮封顶"。boundaryAudit/facts 工具删除，无形式校验面。残留：合同未要求"不选推荐的最强理由"反向论证。**反向论证已采纳（2026-08-19 拍板）**：合同 §6.5 已补"推荐选项必须附不选它的最强理由" | 单测：提问质量合同纯函数；T5 sandbox 话术 | 覆盖（已裁决 2026-08-19） |
| 8-12-2 | 已落账决策无显式撤销/修订路径，改主意成为审计链模糊地带，无 supersede 语义 | 8-12 §2（决策状态表"采纳"） | **覆盖**：合同 §6.5 需求活文档=条目列表，决策并入条目来源字段（带"决策"标记），无状态机无确认字段，条目天然可演进；§5.2 中途需求变更按影响分级吸收（推翻已完成工作的变更才停下说明）。不再需要 supersede 链——没有"锁定生效的决策记录" | test/state.test.ts + test/rebuild.test.ts（条目/决策随事件折叠演进） | 覆盖（已裁决 2026-08-20） |
| 8-12-3 | 决策批量合并候选：多轮 grill 打断次数高 | 8-12 §3（"明确不做"） | **覆盖**：合同 §7 grill 改为原生问答"一次问完一轮封顶"，多轮打断面本身消失；合同 §4.2 首写入门禁"计划摘要确认（一次，不是问卷）" | verify-t5 场景③（完成确认一次，非问卷）+ test/user-prompt.test.ts（识别纪律） | 覆盖（已裁决 2026-08-20） |
| 8-12-4 | coverage theater：每个 AC 必须有 TEST 节点，无行为 AC 被逼出形式测试，Trace 显示"100% 有测试"而实际靠自由文本伪造；TDD 豁免无共享字段 | 8-12 §4（决策状态表"修订后采纳"） | **覆盖**：合同 §9 不强制 TDD；计划 §3.3 `verify:none` 须显式声明（缺省无声明 = done 驳回）、验收=意图块第四要素的 verify 命令。TEST 节点/覆盖图机制删除，无"形式测试"存在空间；容忍的是内容，不是动作（计划 §3.3） | verify-t6 场景①（未跑 verify 就 done → 驳回 fail-visible）+ test/done.test.ts（verify:none 显式声明免验收） | 覆盖（已裁决 2026-08-20） |
| 8-12-5 | planning 校验 throw-on-first，复杂计划登记反复级联往返；失败留孤儿快照；错误详情只给模型看 | 8-12 §5 | **机制已砍失效**：计划文档登记/validate 工具删除（合同 §7 trace/classify 砍掉、§9 不做 WHAT 层；计划 §2.2 工具面=2）。精神由合同 §6.1 强制层承接（deny 报错附模板，fail-visible）；孤儿快照问题随计划文档消失 | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-12-6 | 执行图静态 DAG 刚性：修订通道太贵（abandon→改计划→重登记→重 begin 全量仪式），独立 RU 无法并行，循环依赖被强制线性化 | 8-12 §6（决策状态表"分拆采纳"） | **机制已砍失效**：实施单元/DAG/begin 机制全部删除（合同 §7 分类路线机制砍掉）。痛点由合同 §5.2 承接——需求变更是常态输入，按影响分级吸收，推翻已完成工作的变更才停下一次 | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-12-7 | plan_review attestation 只验形状（host/agentId/raw 非空），模型可自造两份抬到 multi-agent-attested，信任虚高 | 8-12 §7 | **覆盖（机制属第二批）**：合同 §6.3 attestation 原则（"用户已确认"的声称必须由宿主事件证明，模型自述无效）+ §6.4 真实性硬约束（评审独立子代理上下文、结论与代码 sha 绑定、不可变快照）。assurance 档位机制删除 | T 第二批评审单测（6.4 真实性约束）；spike L3 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| 8-12-8 | 未知 diff 全量重审触发原因被吞，角色切片覆盖缺口静默积累，无诊断输出 | 8-12 §8 | **机制已砍失效**：角色切片/role basis 复用机制删除（合同 §6.4 复审只审 blocking 项）。fail-visible 原则由合同 §4.5② 承接（阻断必说清现状、缺什么、怎么解） | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-12-9 | code_review 浅记账：blocking 修复无法核验、scope creep 无归属轴、审查一致性依赖模型状态（无固定基线） | 8-12 §9 | **覆盖（机制属第二批）**：合同 §6.4 双轴审查清单（需求轴三件套对齐 + 规范轴 + 大任务回撤视角）、blocking 必须具体（文件+行+理由，模糊意见无权拦）、复审只审 blocking 项、评审-修复 N 轮不收敛套 §5.3 连败锁定、对抗定位明文。scope creep 由需求轴（需求条目+计划摘要+实际改动三件套）兜住 | T 第二批评审单测 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| 8-12-10a | 手动验收（browser/code-path-audit）无可信事件绑定，模型可编造验收证据；verification 失败修复绕过 code_review 与 checkpoint | 8-12 §10 | **覆盖**：合同 §6.3 三通道完成宣称 + 计划 §3.3 done 时序双检查（验收事件退出码 0 且晚于最后一次写入——代码变则验收失效）；§6.4 评审结论 sha 绑定（代码变结论失效）同源。手动验收模式删除 | verify-t6 场景④（验收通过后改代码 → done 驳回：时序双检查） | 覆盖（已裁决 2026-08-20） |
| 8-12-10b | 验证命令超时/缓冲硬编码（120s/1MB），超时被归一为 exitCode 1，模型无法区分"测试失败"与"超时被杀"，可能修不存在的 bug | 8-12 §10 | **覆盖（2026-08-19 拍板：补）**：计划 §3.3 新增 T6 实现要求——验收事件记账区分退出原因（超时/被杀/非零退出码），复用 PostToolUseFailure 与退出码分离呈现，防"长测试挂死被当失败/通过误判" | T6 sandbox：挂死/超时/非零退出码在验收记账中可区分 | 覆盖（已裁决 2026-08-19） |
| 8-12-11 | quality exception 接受后 governed 文件变化仍放行（fingerprint 不校验时效），"接受风险"演变为"永久豁免" | 8-12 §11 | **机制已砍失效**：quality-exceptions 状态机删除（合同 §7 评审"批次/终态/豁免状态机砍掉"）。承接：合同 §6.4 用户一票终裁权（对话里一句话放行，天然绑定当前时点）+ §4.6 解锁是信息注入（改变后续计数语义），无持久化豁免对象可漂移 | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-12-12 | record_decision 用 `presentedAt=0` 回溯匹配任意历史事件，用户对别的问题说过的同文本可能被记为当前决策证据（语义错配） | 8-12 §12 | **覆盖**：合同 §6.5 需求条目来源字段=用户原话/仓库事实/意图块推断，无 record_decision 人工登记工具（合同 §7 工具面个位数）；§6.3 确认须宿主事件证明。回溯匹配机制删除 | test/user-prompt.test.ts（确认短语精确匹配）+ verify-t5 场景③（宿主事件证明确认） | 覆盖（已裁决 2026-08-20） |
| 8-12-13 | route-confirmation basisHash 不含 project config sha256，确认与落账间配置漂移不失效 | 8-12 §13 | **机制已砍失效**：route-confirmation 门禁删除（合同 §7 classify/lock/reclassify 砍掉；§6.2 无分类动作无锁定无固定路线） | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-12-14 | inspect 与 assertReviewComplete 的 blocking 计算路径不一致（findingEvents vs jobs+dispositions），显示与执行判定分叉 | 8-12 §14 | **机制已砍失效**：inspect/blocking 计算机制删除；只读查询唯一化（计划 §2.2 status 工具，MCP 工具面=2）；状态投影单点（合同 §6.2 治理判定信号单一） | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-12-15 | finalize 不报告 excluded 路径后续内容变化，用户排除后忘了处理也无可见性 | 8-12 §15 | **覆盖（机制属第二批）**：合同 §4.2 防线④ finalize 审计兜底（交付时对比实际变更面与经历治理，不一致显式警告，不阻塞但事后可见）+ §5.1 静默记账。注：finalize 审计渲染在第二批（合同 §12；计划 §8 明示第一批无 finalize 审计渲染为已知边界） | T 第二批 finalize 审计 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| 8-12-16 | 宿主授权记忆（task-reusable 破坏性命令）在 feature 生命周期内永久有效，跨长时间会话后仍自动放行 | 8-12 §16 | **覆盖**：合同 §4.2 防线③ 不可逆操作（git push、删表、发版）永远有人工门禁，与任务大小无关 + 计划 §3.1 PreToolUse(Bash) 拦不可逆操作。授权记忆机制删除，无"记忆过期"问题 | verify-t4 场景④（git push 被拦 write.blocked(irreversible.push)）+ test/write-gate.test.ts（一拦二放） | 覆盖（已裁决 2026-08-20） |
| 8-12-17 | 审查角色/轴级重跑未实现：小修复触发整份双轴重审，成本随代码量放大 | 8-12 §17 | **机制已砍失效**：角色/轴级复用机制删除（合同 §6.4 复审只审 blocking 项，不做全面重审）。"局部变化只重做受影响工作"由 §5.2 变更分级吸收承接 | — | 机制已砍失效（已裁决 2026-08-20） |

---

## 2. 来自 8-13 优化清单（7 条）

出处：`docs/8-13-优化清单.md`（启动与对账域"预存脏文件归属交互"评审，含决策状态表）

| # | 坑描述 | 出处 | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 8-13-1 | 启动时对全部预存脏文件弹归属提问（"路径 X 是否属于当前任务"），大部分多余、强制点又反复打扰 | 8-13 §1（决策状态表"已定：零提问自动纳入"） | **覆盖**：合同 §5.4 文件系统是唯一事实源（启动静默读取真实状态，用户改了吸收进去不打断）+ §5.6 恢复零操作零提问 + §4.2 首写入门禁零问卷直接放行。归属交互机制删除 | verify-t3 场景 A（空状态零提问：意图块规则注入）+ test/briefing.test.ts（空状态播报 null） | 覆盖（已裁决 2026-08-20） |
| 8-13-2 | 任务输入文件（用户 `@` 引用）被当未知路径弹归属提问，明显到荒谬 | 8-13 §2（"已定：无需独立机制"） | **覆盖**：同上——无归属交互概念；意图块四要素含"预计动哪些文件"（合同 §6.1），模型自报即可覆盖任务输入 | verify-t3 场景 A（零提问，无归属交互概念）+ test/briefing.test.ts | 覆盖（已裁决 2026-08-20） |
| 8-13-3 | 归属回答匹配与绑定脆弱：模型自答被拦白烧一轮、用户"确认"不被识别必须精确复述标签、失败后事件消费状态越滚越乱（一题 3 轮往返） | 8-13 §3（"已定：标记已覆盖"） | **覆盖**：归属交互删除（§5.4），脆弱匹配面消失；计划 §3.7 识别纪律（状态翻转只信确认短语的精确匹配——被询问后的下一轮；开放式语义只做温和注入）守住剩余交互的绑定 | verify-t5 场景①②③（逃生门/主线切换/完成确认三通道）+ test/user-prompt.test.ts（精确匹配/温和注入） | 覆盖（已裁决 2026-08-20） |
| 8-13-4 | 启动失败场景状态可见性差：模型分不清 feature 建没建、是否 active，反复 doctor/重建探测，拖成多次往返 | 8-13 §4 | **覆盖**：合同 §4.5② 阻断必 fail-visible（说清现状、缺什么、怎么解）+ §4.6 故障分级；计划 §3.1 SessionStart 恢复播报（"你昨天在做标签功能，做到验证阶段，还差 X"）。启动零提问后此类中间态大幅收缩 | verify-t3 场景 B（恢复播报：主线名/阶段/还差什么）+ test/briefing.test.ts（播报形状） | 覆盖（已裁决 2026-08-20） |
| 8-13-5 | 同一逻辑批准被确认 2-3 次（预览确认 + lock 正式 gate + record_decision 追认），确认疲劳 | 8-13 §5 | **覆盖**：合同 §4.2 计划摘要确认"一次，不是问卷"；§6.3 三通道触发前展示待完成阶段摘要让用户确认一次；§6.5"确认不是字段，是用户在交付对照表上扫一眼那个动作本身"。重复确认交互机制删除 | verify-t5 场景③（完成确认一次，非问卷）+ verify-t6 场景③（done 成功路径）+ test/done.test.ts | 覆盖（已裁决 2026-08-20） |
| 8-13-6 | 事实登记与路线锁定往返低效：并发注册必冲突（乐观 CAS）、ledger ID 反馈不清迫使模型猜 ID 甚至翻源码找合同 | 8-13 §6 | **机制已砍失效**：repository-facts/lock 工具删除（合同 §7 classify/lock/reclassify 砍掉），无账本 ID 合同；意图块自报 + 敏感路径表机器判定（合同 §6.2）替代事实登记 | — | 机制已砍失效（已裁决 2026-08-20） |
| 8-13-7 | grillme 自定义 skill 名未入 cspell 词典，IDE 持续误报；与 marketplace grill-me 命名混用 | 8-13 §7（低置信度，仓库工程问题） | **机制已砍失效**：grillme skill 重做为零提问默认（合同 §7），旧命名体系随旧版文档删除；新版仓库文档待重建（方向合同 §1 历史事实：README/AGENTS/CLAUDE 已删）。命名规范在新文档重建时定 | — | 机制已砍失效（已裁决 2026-08-20） |

---

## 3. 来自 8-18 真实项目清单（11 条 DF-01~11）

出处：`docs/dev-flow-real-project-issue-checklist-2026-08-18.md`（Svelte 与 TanStack 两次真实项目实践；旧测试 `v5-review-isolation-gate`、`v6-review-execution-envelope` 亦固化其中多条）

| # | 坑描述 | 出处 | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| DF-01 | 评审 job 缺少可执行的派发与回收协议：协议跨 MCP/skill/宿主/子智能体四层，start_review_execution 不返回可原样派发的 dispatchPrompt，调用者只能猜 | 8-18 DF-01（P0） | **覆盖（机制属第二批）**：合同 §6.4 评审极简重做——原生 Task 子代理 + SubagentStop hook 把关，协议收敛到宿主事件单一交接物（SubagentStop 载荷含 `last_assistant_message`，计划 §0 已实证）；角色按风险事实派生、对抗定位明文 | spike L1-5 实测 SubagentStop；T 第二批评审端到端 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| DF-02 | 捕获失败静默忽略（adapter 忽略返回值），零 envelope 的 complete 仍返回成功并推进 revision，调度者宣布未完成的评审已完成 | 8-18 DF-02（P0） | **覆盖**：合同 §6.4 真实性硬约束 + §4.5② fail-visible（阻断必说清缺什么怎么解）；完成宣称唯一咽喉 done（计划 §3.3）前置"最近一次验收命令通过"——评审无结果则验收不成立，不存在"空完成"通道；§6.6 证据链零操作自动归集（envelope 由 hook 捕获，无手工提交面） | verify-t6 场景①（验收未过 done 必驳回，无空完成通道） | 覆盖（已裁决 2026-08-20） |
| DF-03 | 用户接受质量例外后 planning 仍被 REVIEW_BATCH_INCOMPLETE 阻断，"接受风险并继续"无法改变真正阻塞条件 | 8-18 DF-03（P0） | **覆盖**：合同 §6.4 用户一票终裁权（"这个问题接受，放行"，一句话生效）+ §7 评审批次/豁免状态机删除——不存在"未完成批次卡住后续"的状态机；合同 §4.5③ 人有一句话终裁权入宪法 | T 第二批评审 sandbox | 覆盖（已裁决 2026-08-20） |
| DF-04 | 状态已提交后仍向调用方返回 INTERNAL_ERROR（后置 seal 失败使整个命令表现为失败），重试制造冲突 | 8-18 DF-04（P0） | **覆盖**：合同 §4.6 故障分级（系统故障一律 fail-open：放行+显眼警告+审计可见；只报警不自修复）；计划 §2.3 状态三文件写权限分工、tmp+rename 原子替换、events 是事实源 state 是缓存（崩溃后 events 重建 state）——不存在"提交后失败"的原子性缺口 | test/state.test.ts（writeState 原子写 tmp+rename）+ test/rebuild.test.ts（events 重建 state，崩溃可恢复） | 覆盖（已裁决 2026-08-20） |
| DF-05 | Read/no-op 被记为可信写入并推进 revision/ownership：读 implementation plan 都增加 revision，污染并发控制与 scope | 8-18 DF-05（P0） | **覆盖**：计划 §3.1 事件映射精确到 matcher——PreToolUse(Write\|Edit\|MultiEdit) 才记写信号；PostToolUse 记账 file.changed；§3.5 记事实不记内容（路径/工具名/退出码）。读操作永不触发写事件。旧测试 write-gate"Read and no-op writes never enter delivery ownership"同源 | test/events.test.ts（file.changed 只记路径/工具名/退出码）+ verify-t4 场景③（意图块声明后写入放行） | 覆盖（已裁决 2026-08-20） |
| DF-06 | 控制不变量失效：路线锁定允许 unit-chain + trace=false + executable-rollback 的不可执行组合，UI 显示"已要求"实际门禁"未启用" | 8-18 DF-06（P0） | **机制已砍失效**：controls/unit-chain/trace 路线机制删除（合同 §7 classify 砍掉；§6.2 XS/S/M/L 保留为度量词汇，无锁定无固定路线）；治理强度由行为推导（§6.2），不存在"声明与执行不一致"的路线组合 | — | 机制已砍失效（已裁决 2026-08-20） |
| DF-07 | 评审批次无完整生命周期（failed/waived/superseded 缺失），旧 batch 永久 open 计入统计，rebuild 无法恢复未捕获输出 | 8-18 DF-07（P1） | **机制已砍失效**：评审批次/batch 机制删除（合同 §7 批次/终态/豁免状态机砍掉）；评审触发简化为三级（§6.4：XS 不审/升级必审/敏感必审），无批次生命周期可管理 | — | 机制已砍失效（已裁决 2026-08-20） |
| DF-08 | record_step evidence schema 过宽（空 schema），JSON 字符串 evidence 进入领域层才报错，且错误不含实际类型与正确示例 | 8-18 DF-08（P1） | **覆盖**：record_step 工具删除（合同 §7 工具面个位数）；计划 §3.5 证据链事件类型 12+1 预算封顶、schema 单点（记事实不记内容），无自由表单可传错类型 | test/events.test.ts（事件类型白名单 12+1 封顶、未知类型拒绝） | 覆盖（已裁决 2026-08-20） |
| DF-09 | 评审并发派发遇 429 配额失败无 fail-fast/限流/可恢复重试，5 个子代理同时失败 | 8-18 DF-09（P1） | **机制已砍失效**：多角色并发派发机制删除（合同 §6.4 评审三级触发 + 角色按风险事实派生，并发面大幅收缩；评审在第二批）；软单主线（§5.7）从源头上没有并行 job 面。无显式限流设计（若第二批引入多角色并发，需回看本坑） | — | 机制已砍失效（已裁决 2026-08-20） |
| DF-10 | repository facts 批量失败缺少定位信息（无 index/expected/actual），调用者只能拆单条探测 | 8-18 DF-10（P2） | **机制已砍失效**：repository-facts 工具删除（合同 §7）；意图块自报 + 敏感路径表机器判定（§6.2/计划 §3.4）替代，无批量登记面 | — | 机制已砍失效（已裁决 2026-08-20） |
| DF-11 | formal plan 路线暴露不可用的 validate_plan 动作（trace=false 时 TRACE_NOT_ENFORCED），把用户引向必然失败 | 8-18 DF-11（P2） | **机制已砍失效**：validate_plan 工具删除（计划 §2.2 工具面=2：done + status；旧测试 v6-tool-contract 同源）；无计划文档需校验 | — | 机制已砍失效（已裁决 2026-08-20） |

---

## 4. 来自 8-16 GPT 审计清单（12 条 GPT-001~012）

出处：`docs/gpt-dev-flow-真实项目问题清单-2026-08-16.md`（5.1.4 在 Svelte/TanStack 审计，与 v4flash/v4pro 交叉验证）

| # | 坑描述 | 出处 | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| GPT-001 | 计划修订不是原子事务：确认时丢弃编译通过的 Trace 提案，agent 必须手动重放 record_artifact_with_trace，产生中间 stale 批与重复审查；预览与登记无法证明同一提案 | 8-16 GPT-001（P0） | **机制已砍失效**：计划文档/Trace 登记机制删除（合同 §7 trace 砍重做为证据链、§9 不做 WHAT 层）。"状态翻转必须原子"由计划 §3.3 done 唯一咽喉承接（hook 不直接翻转状态） | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-002 | Trace 新鲜度空集漏洞：所有 plan 节点均 stale 时"零 current 节点"被误判为通过，next 先调度 review 再查 Trace 放大 | 8-16 GPT-002（P0） | **机制已砍失效**：Trace 图新鲜度机制删除（合同 §6.6 放弃图完整性校验——正是对这类登记/空集漏洞的根治）；完成宣称改由 done 时序双检查（计划 §3.3：验收事件晚于最后写入） | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-003 | Markdown 计划与机器 Trace 在同一 artifact SHA 下静默承载不同语义（file_scope/forwardVerification 分叉），`artifactChanged:false traceChanged:true` | 8-16 GPT-003（P0） | **机制已砍失效**：双输入源删除——合同 §6.1 意图块单一来源（模型生成、用户扫读、hook 硬校验）+ §6.5 需求活文档不落 Markdown（WHAT 层交给 OpenSpec/Spec Kit）。不存在"两份语义漂移"的空间。旧测试 v6-artifact-compiler 同源 | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-004 | SubagentStop 适配器读取宿主不存在的字段（parent_session_id），单测用虚构 schema 假绿，隔离证明无法落账 | 8-16 GPT-004（P0） | **覆盖（机制属第二批）**：计划 §0 事实基线以官方文档实证（SubagentStop 载荷含 `last_assistant_message`）+ §1.1 L1-5 spike 实测真实 schema；§6.4 评审执行基于原生 Task + SubagentStop。spike 纪律（§1.2：报告附精确字段名/载荷样例）防止再虚构 | spike L1-5 必须过；T 第二批评审端到端 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| GPT-005 | plan review 与 code review 共用唯一 current batch，新批次把另一阶段打成 stale，引发审查风暴（TanStack 12 batch/38 job 全重跑） | 8-16 GPT-005（P1） | **机制已砍失效**：评审批次机制删除（合同 §7）；评审触发=三级判断（§6.4），无 batch/current/stale 生命周期 | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-006 | unknownDiff：所有角色 basis 相等时反而禁止全部复用，纯代码变化触发四个 plan 角色全量重审 | 8-16 GPT-006（P1） | **机制已砍失效**：角色复用机制删除（合同 §6.4 复审只审 blocking 项）；评审重量由行为信号定（§6.2），无"未知 diff 全量重审"路径 | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-007 | reconcile 先删 code_review 基准指针、risk acceptance 只存 aggregate SHA，失效传播退化为全量重开 checkpoint（TanStack UNIT-001~005 反复完整重跑） | 8-16 GPT-007（P1） | **机制已砍失效**：reconcile/checkpoint 基准机制删除（合同 §7 rollback/checkpoint 让位 git；§5.4 文件系统是唯一事实源——真实状态直接读，不依赖基准指针）；回撤可操作性降为评审视角（§6.4） | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-008 | MCP JSON Schema 拒绝 Core 已支持的 task.tdd（unknownField），公开工具合同与 Core/skill 分裂，TDD 信息丢失 | 8-16 GPT-008（P1） | **机制已砍失效**：task/TDD 声明机制删除（合同 §9 不强制 TDD；§6.3 验收必须有通过的验证命令）。工具 schema 合同收敛到 2 个工具（计划 §2.2），无手写 schema 可漂移 | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-009 | forward verification 的 targeted 校验过晚（checkpoint 才拦），inline 写法自动获得 provides:["targeted"] 绕过保证 | 8-16 GPT-009（P1） | **覆盖**：计划 §3.3 verify=意图块第四要素（方向债前置）；done 时序双检查（验收事件退出码 0 且晚于最后写入）在完成宣称时刻硬校验，无"边执行边发现"的晚拦；targeted/provides 命令语义机制删除 | verify-t6 场景④（done 时序双检查：验收事件晚于最后写入硬校验） | 覆盖（已裁决 2026-08-20） |
| GPT-010 | batch 宣称 parallel-safe 但 claim/submit 用全局 revision CAS，真实并行必然 STATE_REVISION_CONFLICT | 8-16 GPT-010（P2） | **机制已砍失效**：并行 job/claim 机制删除（合同 §5.7 软单主线同时只一条主线；计划 §2.3 三文件写权限分工、不上锁、同步 append 消竞态） | — | 机制已砍失效（已裁决 2026-08-20） |
| GPT-011 | 长任务状态与不可变证据无体积预算：75MB/10374 文件、state 2.2MB、observedPathFingerprints 1.39MB，live/history/doctor 无分层 | 8-16 GPT-011（P2） | **覆盖**：计划 §2.3 events.jsonl append-only 不轮转（年 MB 级，写进已知边界）+ state 只做缓存（指针/摘要/有界 tail）+ §4 性能/§8 状态体积预算线（token 面收敛，status 不返回全量状态）；旧测试 v6-state-archive 已固化 512KiB 预算——**注记已闭环（2026-08-19 拍板）**：512KiB 状态体积预算线已写入计划 §8（风险与已知边界），不进第一批验收判据 | T2 单测（体积预算） | 覆盖（已裁决 2026-08-19） |
| GPT-012 | 交互失败原因与可见提示不精确：provenance 错误不区分零候选/改写/歧义三种根因，recoveryInstruction 让用户跑 doctor 但 doctor 不修任何东西 | 8-16 GPT-012（P2） | **覆盖**：合同 §4.5② 阻断必 fail-visible（说清现状、缺什么、怎么解）+ §4.6 fail-open 显眼警告 + 计划 §3.3 done 驳回附理由（fail-visible：声明模板 + 用户一句话放行双出口）；识别纪律 §3.7 温和注入 | verify-t6 场景①（done 驳回附理由：无验收通过记录）+ verify-t5 场景③（温和注入） | 覆盖（已裁决 2026-08-20） |

---

## 5. 来自 8-13 架构审查报告（16 条）

出处：`docs/2026-08-13-架构审查报告.md`（9 候选 + 观察/死代码，针对旧版代码结构问题）

| # | 坑描述 | 出处 | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| ARC-1 | 交互落账两套并行合同：answer 只接 8/13 kind，其余 5 个走 9 个导出 resolveX 函数（task-switch resolver 全仓零调用方死代码），错误码与解析入口分叉 | 8-13 候选 1 | **机制已砍失效**：交互落账工具面删除（计划 §2.2 MCP 工具面=2）；用户通道归并到 UserPromptSubmit 事件识别（计划 §3.7），单一入口 | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-2 | MCP「呈现→征询→落账」管道手抄 10 份（10 处 elicit、21 处 interactionEnvelope），每份细微差异是 bug 藏身处；同一 handler 两种返回形状靠猜测链归一 | 8-13 候选 2 | **机制已砍失效**：MCP 工具面从 49 收敛到 2（计划 §2.2），管道副本面消失；事件→机制映射 5 注册点（计划 §3.1），不多挂一个 | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-3 | dispatch 是 1463 行零导出 stdin 脚本，49 个工具唯一测试面是 spawn 进程线协议，interface=传输层 | 8-13 候选 3 | **覆盖**：计划 §4 纯函数纪律（hook 判断逻辑全为纯函数，入口壳只做 stdin 解析+调用+写输出；入口壳行数设预算）+ §1.2 spike 纪律（`claude -p` 脚本化驱动可重复跑）——可测性从设计保证 | test/bash-gate.test.ts（Bash 启发式纯函数用例集）+ verify.sh（入口壳最小场景冒烟） | 覆盖（已裁决 2026-08-20） |
| ARC-4 | 「当前进行到哪一步」四处实现（currentOpenStep/effectiveStage/buildProgress/deriveRouteAction），重分类后只留 satisfied 前缀时投影分叉（已构造反例） | 8-13 候选 4 | **机制已砍失效**：步骤状态机删除（合同 §7 路线机制砍掉；§6.2 无固定路线无分类动作）；治理强度由行为推导单点（§6.2） | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-5 | 路线测试 helper（route-flow.mjs）直调 confirmApproval 等内部 API，13 个测试文件证明的是一条生产永远不走的内部路径 | 8-13 候选 5 | **覆盖**：计划 §4 真机冒烟（sandbox 场景脚本驱动 `claude -p` 走真实插件加载）+ §1.1 L3 假流程用**真实规则文本**——测试路径=生产路径（spike 用真实意图块规则文本验证 fail-visible 真实性） | verify.sh（真实插件加载 + claude -p 冒烟：测试路径=生产路径） | 覆盖（已裁决 2026-08-20） |
| ARC-6 | 计划编译输入装配三处近似重复（preflight/登记/修订），"同输入必同诊断"不变量只由注释维持 | 8-13 候选 6 | **机制已砍失效**：计划编译机制删除（合同 §7） | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-7 | adapter-policy 702 行 module 装三条 seam：句法机私有不可直测、formatter 私有要构造全套状态才能测、编排混入；带 deprecated 双写 | 8-13 候选 7 | **覆盖**：计划 §4 纯函数纪律 + §3.1 Bash 启发式独立实现（正则快路径，命中才继续）+ §8.3 性能预算（代码内快路径）；旧版吸收清单明确"policy 纯函数+组合性质测试"进开发纪律（合同 §11） | test/bash-gate.test.ts（重定向/tee/sed 解析用例集）+ verify-t4 场景②（echo x > .env 被拦） | 覆盖（已裁决 2026-08-20） |
| ARC-8 | begin 就绪判定藏在 mutator 闭包，writeGate 只能"试 begin→重载→重判"，失败压成字符串诊断；"下一就绪单元"搜索两处重复 | 8-13 候选 8 | **机制已砍失效**：begin/实施单元机制删除（合同 §7）；写门禁简化为首写入门禁强制层（合同 §6.1：意图块存在性+敏感路径声明硬校验） | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-9 | policy 层死约定宽度：controlsOrCapabilities union 分派、证据派生两条会分歧的通道、contradictions 恒空数组、fallback 映射表写两遍 | 8-13 候选 9 | **覆盖**：合同 §4.3 复杂度预算（工具数/错误码数/模块数设上限，超限必须显式说明删了什么）+ 计划 §4 纯函数纪律——从设计上防止死约定堆积 | test/bash-gate.test.ts + test/state.test.ts | 覆盖（已裁决 2026-08-20） |
| ARC-10 | mutate 的 operation 事件类型是 67 处调用点自由命名的裸字符串，无注册表，审计词汇有漂移风险 | 8-13 观察 | **覆盖**：计划 §3.5 事件类型 12+1 预算封顶、schema 单点（新增必答三问）；记事实不记内容 | test/events.test.ts（事件类型 12+1 白名单封顶、未知类型拒绝） | 覆盖（已裁决 2026-08-20） |
| ARC-11 | obligations 角色死词汇（"data-integrity"不在 ReviewRole 词表），`roles: string[]` 类型宽度让死词汇漏过检查 | 8-13 观察 | **机制已砍失效**：obligations/评审角色表机制删除（合同 §6.4 评审极简；角色按风险事实派生而非预定义词表） | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-12 | 测试按年代分桶（v2/v3/v5），57/89 文件直连 god store，测试面事实中心是 store 而非领域 interface | 8-13 观察 | **覆盖**：计划 §4 测试纪律（policy 纯函数全覆盖 + 敏感路径性质测试 + sandbox 真机冒烟）+ §5 坑清单机制（新旧坑同一张表持续闭环） | verify.sh + verify-t3~t8（真机冒烟全链）+ test/*.test.ts（纯函数单测面） | 覆盖（已裁决 2026-08-20） |
| ARC-13 | validateFeatureState 与 TS 类型、policy schema、server 工具 schema 多种表示同一合同，只靠抽查测试维系同步 | 8-13 观察 | **覆盖**：合同 §4.3 additive-only 状态契约（字段只增带默认值，冷冻点前自由改）+ 计划 §2.3 读 fail-open（非法 JSON=系统故障）+ §4 纯函数纪律；状态 schema 合同单点化 | test/state.test.ts（additive-only 读端/未知字段容忍）+ test/rebuild.test.ts（events 重建 state） | 覆盖（已裁决 2026-08-20） |
| ARC-14 | deriveReviewInput 每次全量读工件+全根指纹+双读 config，经 status→next→reviewGate 链高频触发，成本无量化 | 8-13 观察 | **覆盖**：合同 §8.3 性能预算（P95 生死线，含只读缓存、token 面收敛、代码内快路径）+ 计划 §1.1 L2-3 spike 实测 P95 → 验收判据（§7.4） | scripts/perf-fastpath.mjs（100 次冷启动抽测）+ build/bash-fastpath-p95.txt（实测 p95=17.9ms ≤30ms，判据4 达标） | 覆盖（已裁决 2026-08-20：build/bash-fastpath-p95.txt p95=17.9ms，判据4 达标） |
| ARC-15 | task-switch 手写 CAS 呈现绕开统一 mutate 模板（纪律偏离），增加锁模型变化时的维护风险 | 8-13 观察（并发丢写窗口已撤回，残留纪律问题） | **机制已砍失效**：task-switch 机制删除（合同 §5.7 软单主线切换零仪式——"先弄那个"一句话挂起/激活，状态保留） | — | 机制已砍失效（已裁决 2026-08-20） |
| ARC-16 | 版本号多源不同步：历史 10+ 次版本同步事故（插件版本、package.json、dist 缓存各自为政） | 合同 §8.4（旧版历史事实） | **覆盖**：计划 §2.2 版本单源——plugin.json 不设 version（文档证实可选），分发产物版本由构建脚本从 package.json 注入，发布一条命令 + CI 校验；旧测试 version-sync 同源 | scripts/build.mjs（esbuild --define 注入 DEV_FLOW_VERSION，版本单源）+ verify.sh（版本注入生效） | 覆盖（已裁决 2026-08-20） |

---

## 6. 来自 main 分支旧版测试（28 条）

> 旧版 107 个单元测试是"踩坑记录"（方向合同 §10.6：旧 107 个单元测试作为坑清单逐条核对）。本节从测试用例骨架提炼"这个测试在防什么坑"，按领域聚合，标注代表性测试路径。与 §3/§4 已收录的同源坑不重复建单（在对应条目已注明）。

| # | 坑描述（测试在防什么） | 出处（代表性旧测试） | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| T-01 | 确认必须绑定真实宿主事件：later-turn 证明（呈现之后的用户事件才有效）、同宿主才可消费、事件不可重放、文本不匹配即拒绝 | `tests/unit/v2-host-event-provenance.test.mjs`、`tests/unit/interaction-answer.test.mjs`、`tests/unit/v5-decision-ratification.test.mjs`、`tests/unit/v5-trusted-acceptance.test.mjs` | **覆盖**：合同 §6.3 attestation 原则（"用户已确认"须宿主事件证明，模型自述无效）+ 计划 §3.7 识别纪律（状态翻转只信确认短语精确匹配——被询问后的下一轮） | verify-t5 场景③（用户通道完成确认：宿主事件证明）+ test/user-prompt.test.ts（确认短语精确匹配） | 覆盖（已裁决 2026-08-20） |
| T-02 | 确认短语精确匹配、多意图不误伤："确认"不能命中 workspace-ownership 的 adopt/exclude，否定前缀文本（"等等"）绝不能被当确认 | `tests/e2e/interaction-semantic-resolution.test.mjs`、`tests/unit/v2-approval.test.mjs`、`tests/unit/interaction-answer.test.mjs` | **覆盖**：计划 §3.7 识别纪律——开放式语义只做温和注入（模型多问一句），状态翻转只信确认短语精确匹配；不误判不擦除用户 prompt | verify-t5 场景③ + test/user-prompt.test.ts（否定前缀不误判、多意图不误伤） | 覆盖（已裁决 2026-08-20） |
| T-03 | 交互失败不写任何东西：文本凭证绑定失败时 revision 不变、事件未消费、问题仍 pending；CAS 冲突整笔不写 | `tests/unit/interaction-answer.test.mjs`、`tests/unit/v6-interaction-answer.test.mjs` | **覆盖**：计划 §3.3 done 硬校验（驳回记 done.rejected，状态不翻转）+ §2.3 tmp+rename 原子替换 + §4.6 故障分级 | test/state.test.ts（writeState 原子写 tmp+rename）+ test/done.test.ts（驳回不翻转状态）+ verify-t6 场景① | 覆盖（已裁决 2026-08-20） |
| T-04 | attestation 真实性：free-text 无来源证明永远不抬 multi-agent 档；同一 raw 不可跨 job/跨批次复用；`verified` 声称不可由调用方报告 | `tests/unit/v5-review-source-labels.test.mjs`、`tests/legacy-v1/unit/review-identity.test.mjs`、`tests/unit/v5-governance-contract.test.mjs` | **覆盖（机制属第二批）**：合同 §6.3 attestation 原则（确认须宿主事件证明）+ §6.4 真实性硬约束（独立子代理上下文、sha 绑定、不可变快照）；assurance 档位机制删除 | T 第二批评审单测 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| T-05 | 评审隔离证明不可自证：review-execution 事件不暴露为 agent 可调工具（防 self-attestation）；SubagentStop 只对已声明 job 且上下文 distinct 才记录 | `tests/unit/v5-review-isolation-gate.test.mjs` | **覆盖（机制属第二批）**：合同 §6.4 真实性硬约束（评审必须独立子代理上下文，防自审自）+ 计划 §2.2 工具面=2（无自证工具可调） | T 第二批评审单测 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| T-06 | 零结果/空 envelope 不得伪成功：零 envelope complete 不推进 revision，必须至少一个合法 envelope 或显式终止 | `tests/unit/v6-review-execution-envelope.test.mjs` | **覆盖**：计划 §3.3 done 验收硬前置（验收未过必驳回，缺 verify 声明也驳回）；完成宣称不存在"空完成"通道 | verify-t6 场景①（缺 verify 声明也驳回）+ test/done.test.ts（无声明→驳回） | 覆盖（已裁决 2026-08-20） |
| T-07 | 评审派发 prompt 可原样转发：dispatchPrompt 不加工即被子代理完成并捕获；completion schema 只在一个核心模块定义 | `tests/unit/v6-review-execution-envelope.test.mjs` | **覆盖（机制属第二批）**：合同 §6.4 原生 Task 子代理 + SubagentStop hook；计划 §0 已实证 `last_assistant_message`；协议单一交接物 | spike L1-5；T 第二批 | 未吸收（已裁决 2026-08-20：覆盖机制属第二批，第一批未建） |
| T-08 | 只读工具与 no-op 写不进 ownership/revision：Read 任意受管文件前后 revision/ownership 不变；写工具但 hash 未变也不推进 | `tests/unit/write-gate.test.mjs`、`tests/unit/delivery-snapshot.test.mjs` | **覆盖**：计划 §3.1 matcher 精确（Write\|Edit\|MultiEdit 才记写信号）+ §3.5 记事实不记内容（file.changed 只记路径/工具名/退出码） | test/events.test.ts（file.changed 只记事实不记内容）+ verify-t4 场景③（意图块声明后写入放行） | 覆盖（已裁决 2026-08-20） |
| T-09 | 状态体积预算：10k owned 路径 state 保持 <512KiB；历史证据冷/热分层、可归档 | `tests/unit/v6-state-archive.test.mjs`、`tests/unit/v6-event-segments.test.mjs`、`tests/unit/v6-evidence-maintenance.test.mjs` | **覆盖**：计划 §2.3（events append-only 不轮转、state 只做缓存）+ §8 状态体积预算线 512KiB（防漂移、不进第一批验收判据）；注记同 GPT-011 已闭环 | T2 单测 | 覆盖（已裁决 2026-08-19） |
| T-10 | 版本同步：manifest/插件包/构建产物版本必须一致 | `tests/unit/version-sync.test.mjs` | **覆盖**：计划 §2.2 版本单源（见 ARC-16，同源） | scripts/build.mjs（版本单源注入 DEV_FLOW_VERSION）+ verify.sh | 覆盖（已裁决 2026-08-20） |
| T-11 | 插件根路径含空格时 hook 命令必须正确引号；子目录 cwd 下事件落入根账本；跨宿主中文/分解 Unicode 路径同一 NFC 表示 | `tests/unit/host-paths.test.mjs`、`tests/unit/v5-hook-subdir-root.test.mjs`、`tests/e2e/cross-host/v2-intake-handoff.test.mjs` | **覆盖（空格/子目录）**：计划 §0 事实基线（`CLAUDE_PROJECT_DIR` 注入 hook 进程）+ §2.3 状态根=CLAUDE_PROJECT_DIR；**覆盖（NFC，2026-08-19 拍板：补）**：计划 §3.4 敏感路径匹配新增 NFC 归一化 + symlink 目标解析（T4 实现要求，N-5 已闭环） | T1/T3 单测 + sandbox；T4 NFC/symlink 单测 | 覆盖（已裁决 2026-08-19） |
| T-12 | 授权每次执行重新确认：成功 PostToolUse 只留审计，相同命令再次执行仍 defer，不生成可复用 grant | `tests/unit/v2-host-authorization.test.mjs`、`tests/unit/v2-host-adapter-protocol.test.mjs` | **覆盖**：合同 §4.2 防线③ 不可逆永远人工门禁（与任务大小无关）+ 计划 §3.1 PreToolUse(Bash) 拦不可逆操作（push/删表/发版） | verify-t4 场景④（push 必人工门禁，无复用授权）+ test/write-gate.test.ts（一拦二放） | 覆盖（已裁决 2026-08-20） |
| T-13 | 事件账本损坏时的安全姿态：批准增强信息账本损坏时保持 unreadable，不 fail-open protected 写入 | `tests/unit/write-gate.test.mjs`、`tests/unit/v5-governance-schema.test.mjs`、`tests/unit/doctor.test.mjs` | **覆盖（有意识方向反转）**：合同 §4.6 故障分级裁决——系统故障一律 **fail-open**（放行+显眼警告+审计可见；只报警不自修复；重建只在 doctor 手动触发先备份）。与旧版 fail-closed 相反，属设计明示的裁决而非遗漏（"门卫晕倒不该把楼锁了"）；**2026-08-19 已确认：fail-open 与隐形治理宪法（合同 §4.1）自洽，方向成立** | spike L2 实测 | 覆盖（已裁决 2026-08-19） |
| T-14 | git commit -a/-am/--all 无法安全枚举，即使已批准也按 unbounded 拦下；预存脏文件手动 git add 时提示不拦但未知/排除路径仍拦 | `tests/unit/v2-adapter-overlay.test.mjs`、`tests/unit/git-policy.test.mjs` | **覆盖**：计划 §3.6 选择性提交——自动 commit 只 `git add` events 里 file.changed 记录的主线触碰文件清单，**绝不 commit -am**（用户自有改动一个不卷进来）；手动 commit 属旁路自由（合同 §5.1），防作弊靠 finalize 审计（§4.2 ④） | verify-t7 场景①（done 后 git log 只含主线文件、用户改动不卷入）+ test/auto-commit.test.ts（选择性提交）+ test/mcp-server.test.ts（done 通过路径接线） | 覆盖（已裁决 2026-08-20；verify-t7 全量回归 2026-08-20 补跑 6/6 通过：自动提交只含主线文件实锤 commit；autoCommit=false 时 done 照成无提交+audit.warning） |
| T-15 | 打开的回滚事务 journal 阻断一切 feature mutation，直到事务完成 | `tests/unit/rollback-journal-gate.test.mjs`、`tests/unit/interaction-answer-rollback.test.mjs` | **机制已砍失效**：rollback 事务系统删除（合同 §7 rollback/checkpoint 让位 git，§9 不做回滚事务系统） | — | 机制已砍失效（已裁决 2026-08-20） |
| T-16 | symlink 处理：仓库内受管 symlink 穿越 checkpoint 并在 rollback 时原子还原；symlink 从 governed-root 枚举中排除 | `tests/e2e/symlink-checkpoint-rollback-v5.test.mjs`、`tests/unit/v2-fingerprint.test.mjs` | **机制已砍失效（checkpoint/rollback 部分）**：checkpoint/rollback 机制删除（合同 §7）；**symlink 残余已闭环（2026-08-19 拍板：补）**：计划 §3.4 敏感路径匹配新增 symlink 目标解析（N-5 已闭环，敏感路径匹配面覆盖） | — | 失效（已裁决 2026-08-19） |
| T-17 | legacy schema 硬切换 fail-closed：v4/v5 旧状态被 6.0 解析器硬拒（无迁移路径）；v1~v3 稳定 unsupported 错误 | `tests/unit/v5-legacy-fixtures.test.mjs`、`tests/unit/v5-governance-schema.test.mjs`、`tests/unit/v2-state-schema.test.mjs`、`tests/unit/v3-state-schema.test.mjs` | **覆盖**：合同 §4.3 additive-only 状态契约（字段只增带默认值；**生效时点=冷冻点 1.0.0**，冷冻前自由改不承诺兼容；冷冻后只能删着改不能推倒改）——"每次大版本删旧合同硬切换"的历史病被宪法禁止（§10.7 防复发） | 人审确认冷冻点判据（基准全绿+5 真实任务） | 待审（待 T10：冷冻点判据人审） |
| T-18 | TDD 声明与豁免：task.tdd（test-first/direct）必须到达机器 Trace；non-behavior 处置须附非空理由否则拒绝 | `tests/unit/v5-verification-dispositions.test.mjs`、`tests/unit/v2-skill-contract.test.mjs` | **覆盖**：合同 §9 不强制 TDD（XS 任务全套测试成本可能高于收益）+ 计划 §3.3 `verify:none` 须显式声明（宽容的是内容，不是动作） | verify-t6 场景①（verify:none 未声明/未跑 done 必驳回）+ test/done.test.ts（verify:none 显式声明免验收） | 覆盖（已裁决 2026-08-20） |
| T-19 | 质量例外/风险接受绑定：接受须用户 comment、绑定当前内容指纹、内容变化即失效重开验证 | `tests/unit/v5-risk-acceptance-binding.test.mjs`、`tests/unit/v5-risk-acceptance-elicitation.test.mjs`、`tests/unit/v3-quality-exceptions.test.mjs` | **覆盖**：合同 §6.4 用户一票终裁权（对话一句话放行）+ §4.6 解锁是信息注入（用户给的上下文改变后续计数语义）——无持久化例外对象，天然绑定当前时点 | verify-t5 场景①（逃生门：用户放行记账）+ test/user-prompt.test.ts（解锁信息注入） | 覆盖（已裁决 2026-08-20） |
| T-20 | 状态恢复与 doctor：损坏状态可安全 abandon/备份重建，重建不重写主证据；open journal 时禁止新 start | `tests/unit/doctor.test.mjs`、`tests/unit/state-recovery.test.mjs`、`tests/unit/v5-finalized-recovery.test.mjs` | **覆盖**：合同 §4.6（重建只在 doctor 手动触发且先备份；只报警不自修复）+ 计划 §2.3（events 是事实源、state 是缓存，doctor 重建数据基础） | test/rebuild.test.ts（events 重建 state）+ test/state.test.ts（读 fail-open） | 覆盖（已裁决 2026-08-20） |
| T-21 | events 可重建 state：崩溃后从不可变事件恢复指针/新鲜度，不依赖主证据重写 | `tests/unit/v5-finalized-recovery.test.mjs`、`tests/unit/v6-event-segments.test.mjs` | **覆盖**：计划 §2.3 events.jsonl 为事实源、state 为缓存（崩溃不一致时以 events 重建 state） | test/rebuild.test.ts（events 为事实源折叠重建） | 覆盖（已裁决 2026-08-20） |
| T-22 | 逃生门：用户"急，直接改"放行但记账 | `tests/unit/v2-adapter-overlay.test.mjs`（escape 相关路径） | **覆盖**：合同 §5.5 逃生门（放行+记 escape.used+审计可见）+ 计划 §3.7 | verify-t5 场景①（逃生门：放行 + escape.used 落账） | 覆盖（已裁决 2026-08-20） |
| T-23 | 主线切换：新任务从不静默切换 active feature，task-switch 走显式回答且状态保留 | `tests/unit/task-switch-answer.test.mjs`、`tests/unit/v3-lifecycle.test.mjs` | **覆盖**：合同 §5.7 软单主线（"先弄那个"→当前主线挂起状态保留、新主线激活；恢复走 §5.6）+ 计划 §3.7 | verify-t5 场景②（主线切换：挂起保留/激活/恢复） | 覆盖（已裁决 2026-08-20） |
| T-24 | 原生问答解析：AskUserQuestion 回答事件带问题文本、拒绝回答未显示的问题、结构化回答与文本回答双形态 | `tests/unit/v2-claude-native-question.test.mjs`、`tests/e2e/structured-credential.test.mjs` | **覆盖**：计划 §0 事实基线（UserPromptSubmit 载荷含用户原始 prompt 文本）+ §3.7（用户通道完成确认；识别纪律温和注入） | verify-t5 场景③（用户通道完成确认）+ test/user-prompt.test.ts（UPS 载荷识别） | 覆盖（已裁决 2026-08-20） |
| T-25 | 工具与技能合同一致：MCP 公开面与 Core 校验、skill 描述三方对齐（v6 工具面已收敛） | `tests/unit/v6-tool-contract.test.mjs`、`tests/unit/v2-tool-schema-contract.test.mjs`、`tests/unit/v2-skill-contract.test.mjs` | **覆盖**：计划 §2.2 工具面预算=2（新增工具必答三问，不会长回几十个）+ §4 单测；skills/commands/agents 不进第一批（§2.2） | verify-t8 场景①（status 真实会话调用，含主线/验收/宣称摘要且 ≤500 字符）+ test/status.test.ts + test/mcp-server.test.ts（done/status 双工具注册接线） | 覆盖（已裁决 2026-08-20；verify-t8 全量回归 2026-08-20 补跑 7/7 通过：status 输出 479 字符 ≤500、只读不改状态） |
| T-26 | 错误呈现：中文错误、内部路由码不泄漏为唯一标签；policy 校验失败带具体码而非 INTERNAL_ERROR | `tests/unit/v3-error-presentation.test.mjs` | **覆盖**：合同 §4.5② fail-visible（说清现状、缺什么、怎么解）+ 计划 §0（permissionDecisionReason 模型可见，fail-visible 成立） | verify-t4 场景①④（deny 理由进 transcript 可见）+ spike 结论 L3（docs/2026-08-19-spike-结论.md） | 覆盖（已裁决 2026-08-20） |
| T-27 | 决策 basis 确定性：同一决策依据只映射一个哈希（对象键序无关） | `tests/unit/v2-interruption-budget.test.mjs`、`tests/unit/v2-risk-obligations.test.mjs` | **覆盖**：合同 §11 吸收清单（policy 纯函数 + 组合性质测试进开发纪律）+ 计划 §4 单测（敏感路径匹配性质测试） | test/sensitive-paths.test.ts（性质测试：任意输入不崩溃、结果稳定）+ test/bash-gate.test.ts（解析确定性） | 覆盖（已裁决 2026-08-20） |
| T-28 | 未初始化/配置损坏的可操作错误：not-initialized 与 INVALID_PROJECT_CONFIG 分开；intake 期工具调用不崩溃 | `tests/unit/v3-lifecycle.test.mjs` | **覆盖**：计划 §2.1 node 存在性为安装期前置条件（doctor 检查 + fail-visible）+ §2.3 读 fail-open | verify.sh（node 存在性前置检查）+ test/state.test.ts（读 fail-open） | 覆盖（已裁决 2026-08-20） |

---

## 7. 未吸收坑汇总（5 条，2026-08-19 全部拍板）

| # | 坑描述 | 出处 | 未吸收理由 | 风险 | 建议 |
| --- | --- | --- | --- | --- | --- |
| ⚠ N-1 | **敏感路径覆盖度残余风险**：内置四类窄表必然漏项目自定义敏感路径，第一批只有项目配置补充兜底，finalize 审计启发式在第二批 | 合同 §10.3（自承认"承认不归零"） | 设计明示不归零；第一批漏报时无任何机制提示（计划 §3.4"第一批已知边界：漏报兜底只有项目配置"） | **高**：漏判=敏感写入（密钥/CI/数据）无拦截无升级，靠用户与模型自觉 | **已裁决（2026-08-19）：接受风险**。窄表+项目配置是做减法的合法取舍，finalize 审计第二批收口，第一批不加机制。 |
| ⚠ N-2 | **蓄意绕过 / 自欺声明防不住**：`echo ok` 式假验收、假意图块声明，锁定防的是无意识空转，防不住对抗 | 合同 §10.4、计划 §3.3（"自欺残余风险"） | 设计裁决：蓄意绕过任何系统都防不住，防线在评审独立性、交付审计与用户终裁，不加机制 | **高**：验收硬校验被廉价伪造时"可证明"北极星被架空（对 MVP 影响小——第一批靠用户扫读兜底，二批评审后才成立防线） | **已裁决（2026-08-19）：接受诚实账**。防线在第二批评审独立性+用户终裁，不加机制。 |
| ⚠ N-3 | **解释器任意写入判不出来**：`python -c`、node -e 等任意代码写入既不经 Write matcher 也不命中 Bash 启发式正则 | 计划 §8 风险 2（"写入覆盖的残余缺口"） | 设计明示：无法静态判定，属蓄意绕过类；常见写入模式由 Bash 启发式覆盖，最坏回退"评审+审计兜底" | **中-高**：写入门禁存在真实覆盖缺口（但无害的正常解释器写入也因此被放行，代价是漏网而非误拦）；子代理内写入 spike L2-2 已实测无盲区（计划 §0） | **已裁决（2026-08-19）：接受为明示残余**。spike L2-2 已证事件层面无盲区（写入路径全覆盖），语义缺口属蓄意绕过类，与 N-2 同源处理。 |
| N-4 | **验收命令超时/退出原因不可区分**：长命令挂死或超时被杀与普通失败无法区分，done 判定无依据 | 8-12 §10（及旧测试 `v5-verification-exit-reasons`） | 新版只设计"验收事件退出码 0 且晚于最后写入"（计划 §3.3），验收命令执行机制无超时/退出原因设计 | 中：集成测试挂死时模型可能盲目重试或误判通过；连败计数被挂死命令污染 | **已裁决（2026-08-19）：补**。T6 验收事件记账区分退出原因（超时/被杀/非零退出码，复用 PostToolUseFailure 与退出码分离呈现），已写入实施计划 §3.3/T6；正文 8-12-10b 行已同步。 |
| N-5 | **路径规范化与文件系统特例（NFC/symlink）未设计**：macOS 文件系统常存分解（NFD）路径，敏感路径表 glob 与写入门禁路径匹配可能漏判/误判；敏感路径匹配与 Bash 启发式对 symlink 路径的行为亦无设计。旧版测试固化"实现路径 NFC 且平台无关"、symlink 从 governed-root 枚举排除、仓库内 symlink 穿越检查点原子还原 | 旧测试 `v2-intake-handoff`、`v2-fingerprint`、`rollback-policy`、`delivery-snapshot`、`symlink-checkpoint-rollback-v5` | 新版设计未出现路径规范化要求（§3.4 敏感路径表、§3.1 Bash 启发式均只谈模式匹配）；symlink 相关机制（checkpoint/rollback）已删除，但路径表匹配本身仍需定义行为 | 低-中：中文/韩文等路径下敏感路径拦截可能静默漏掉；Bash 启发式对引号/转义中的分解字符解析偏差；symlink 指向敏感目标时匹配语义未定义 | **已裁决（2026-08-19）：补**。T4 敏感路径匹配做 NFC 归一化 + symlink 目标解析（§4 单测加两条用例），已写入实施计划 §3.4/§4/T4；正文 T-11、T-16 引用处已同步。 |

---

## 8. 排除说明（明确不收录，防审阅者误判"漏了"）

以下内容经过核对**刻意不建单**，理由如下：

1. **非坑决策项**：8-12 §3 决策批量合并（"明确不做，保持一次一个交互"）已并入 8-12-3 覆盖条目；架构审查"暂缓项"（rollback 拆分、49 工具面收敛、第二意见收编）已被 ARC-2/ARC-15 覆盖或随机制删除；"值得做但已实施"四条（types 归位、interactions 类型化、双宿主去重、参数校验收拢）与"死代码速删"是旧版 6.0 已完成的内部整理，重写后无对应面。
2. **已反证/非缺陷**（8-16 交叉验证段）：JSON 键序致 UNIT 误判（反证，键序无关由 T-27 承接）；TanStack 最后 checkpoint 是 cwd 问题（反证，抓到了真实回归）；route confirmation 首次失败是模型改写用户原话（fail-closed 正确，错误分型已并入 GPT-012）；plan 修订后重新批准属预期安全约束。
3. **环境/项目配置问题**：Playwright 缺失、`full-ci` 超时与过宽、依赖预检——非 Dev Flow 缺陷。
4. **评审内容质量问题**：审查者领域误判、性能 pre-change 基线缺失——agent/计划质量问题，新版"评审职责是找茬"（§6.4 对抗定位）与意图块 verify 声明（§3.3）从流程面回应，但领域判断正确性不是流程能保证的。
5. **设计明示的合法取舍**：多宿主推迟（合同 §9）、Windows 不在第一批（计划 §2.1）、不做规范文件管理/不做 dashboard/不做多任务并行（合同 §9）、MVP 期大任务非敏感部分无升级评审（合同 §12 明示已知边界）、TDD 不强制（合同 §9）——均为拍板决策而非坑。
6. **8-18 纠偏段**：REVIEW_BATCH_INCOMPLETE 等是 DF-01/02 下游症状（并入对应条目，不重复建单）；429 配额是外部资源（并入 DF-09）；"定时自动关闭批次=自动通过"是错误终态（批次机制已删）。

---

## 9. 审阅指引（T10 前人工过一遍）

> **2026-08-19 拍板进展**：本轮 8 条已拍板——N-1（接受风险）、N-2（接受诚实账）、N-3（接受明示残余）、N-4（补：T6 退出原因记账）、N-5（补：T4 NFC/symlink）、T-13（确认 fail-open）、GPT-011/T-09（512KiB 预算线写入计划 §8，不进第一批验收判据）、8-12-1（反向论证采纳，写入合同 §6.5）。对应条目状态已闭环。
> **2026-08-20 核销进展**：T0~T8 实现完成；真机 verify.sh / verify-t3 / t4 / t5 / t6 五场景全过（t6 含超时转后台修复：场景⑤ 实证 timeout+backgroundTaskId 落账）；t7/t8 接线完成、verify-t7/t8 脚本就绪。**2026-08-20 全量串行回归已补跑完成，全部通过**——`npm test` 218/218 全绿；verify-t7 6/6（自动提交只含主线文件实锤 commit；autoCommit=false 时 done 照成无提交+audit.warning）；verify-t8 7/7（status 输出 479 字符 ≤500；只读不改状态）；同轮 verify-t3/t4/t5/t6 全绿（verify-t6 场景⑤ 确认 exitReason=timeout + backgroundTaskId 落账、done 不误放行）；T-14/T-25 状态列已同步核销。状态列按三类闭环：A 类 66 条按机制裁决核销（验证方式引用真实文件/测试名）、B 类 11 条按对应 verify 场景核销（T-17 保持待 T10）、C 类 8 条显式标"未吸收（覆盖机制属第二批，第一批未建）"；ARC-14 P95 判据达标（build/bash-fastpath-p95.txt p95=17.9ms ≤30ms）。**状态列「待审」仅剩 T-17**（待 T10 冷冻点判据人审）。

1. 逐条核对三处：**出处真实存在**（点开原文）→ **覆盖机制引用到具体小节**（合同/计划里找得到）→ **验证方式可执行**（对照 T1~T8 判据）。
2. 重点已过：§7 五条未吸收已于 2026-08-19 全部拍板——N-1/N-2/N-3 接受（结论标注在 §7 表），N-4/N-5 补机制（已写入计划 T6/T4）。
3. 重点裁决复核已确认：T-13 fail-open 与隐形治理自洽（已闭环）、N-2 诚实账接受、N-4/N-5 补机制（均已闭环）。
4. 残留注记两处已拍板（2026-08-19）：GPT-011/T-09 的 512KiB 体积预算写入计划 §8 预算线（不进第一批验收判据）；8-12-1 反向论证采纳，已写入合同 §6.5 提问质量合同。
5. 状态列审定时从"待审"改为"覆盖/失效/未吸收（已裁决）"三态闭环；新版自己踩的新坑持续收录本表（计划 §5）。

---

## 10. 新版自踩坑（持续收录，计划 §5）

| # | 坑描述 | 出处 | 新版覆盖机制 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| NEW-1 | Bash 超时宿主转后台被记 verify.passed（挂死被归一为通过）：2.1.234 sdk-cli 下 Bash 超时不再走 PostToolUseFailure，而是命令转后台走 PostToolUse 成功路径（tool_response 带 timedOutAfterMs/backgroundTaskId），buildVerifyEvent 误记 verify.passed（exitCode 0） | verify-t6 场景⑤ 2026-08-20 实证（连续 3 次失败，载荷探针抓包） | **覆盖**：buildVerifyEvent PostToolUse 成功路径新增转后台判定——timedOutAfterMs 是 number → verify.failed + exitReason=timeout；仅 backgroundTaskId 非空（模型主动转后台）→ verify.failed + exitReason=unknown；两者均带 backgroundTaskId 落账。verify.passed 必须有退出码 0 实证，转后台无完成证据绝不可记通过；旧 PostToolUseFailure "Command timed out after" 分支保留兜底 | verify-t6 场景⑤（断言改核 backgroundTaskId 落账）+ test/verify.test.ts 新用例 | 覆盖（已确认 2026-08-20：verify-t6 场景⑤ 已过，timeout+backgroundTaskId 落账实证） |
| NEW-2 | verify.sh 硬编码 `scripts/empty-settings.toml`：cc-switch/Kimi 代理指向失效 provider 时，会话连不上，首条断言被模型噪音/连接错误污染，验证环误判 | 2026-08-20 全量回归实测 | **覆盖**：会话 settings 改为 `${DEV_FLOW_TEST_SETTINGS:-$ROOT/scripts/empty-settings.toml}`（env 可覆盖，见 scripts/make-test-settings.sh），验证环可显式指向直连 provider 兜底 | verify.sh + verify-t3~t8（所有验证脚本同款 SETTINGS 取值） | 覆盖（已裁决 2026-08-20） |
| NEW-3 | verify-t4 `latest_transcript` 用 `ls \| head` 管道取首行：macOS BSD `ls` 输出多时 `head` 提前关读端 → `ls` 收 SIGPIPE 退出码 141 → 命令替换在 `set -o pipefail` 下让脚本静默死（非零退出） | verify-t4 场景重跑实测（2026-08-20） | **覆盖**：改为命令替换 + `${out%%$'\n'*}` 取首行（无管道无 SIGPIPE 面）；verify-t8 同款实现（注释"不用管道取首行"）已固化 | verify-t4 全场景 + verify-t8（同款 latest_transcript） | 覆盖（已裁决 2026-08-20） |
